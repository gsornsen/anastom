# M2.5 Claude Code worker evidence

Status: **completion evidence ready for owner review.** The implementation, native gates, provider-backed unchanged Task, durable replay and artifact checks, path-policy decision, and final-code CI are complete. The owner approved the code in [PR #21](https://github.com/gsornsen/anastom/pull/21) and permanently confirmed contribution rights. Review of this completed evidence record is the remaining milestone gate.

## Identities and environment

The owner accepted the design and [ADR 0015](adr/0015-claude-code-worker-profile.md) in [PR #20](https://github.com/gsornsen/anastom/pull/20), merged as `1ffaffc4ab84b39d3a2916a74bd1bd38d9bcd437`. The signed-off implementation checkpoints are terminology commit `115984cabea41d424689a5834162adfbfbcdb695`, source adapter `a0f98d9f5a62aa82920449a140e74515490d823c`, scoped path policy `9fc321a`, CodeQL remediation `8b06d4d278cd080469d56001d86476be2b10de62`, and deterministic Codex cleanup `e20605988116159633dadc56209e47a226b9bb24`. PR #21 is the implementation review identity. [ADR 0016](adr/0016-path-policy-after-third-adapter.md) records the reviewed shared path boundary.

The local audit ran on 2026-09-15 with Node.js `26.4.0`, pnpm `11.9.0`, macOS Darwin `25.2.0` on arm64, and end-user-installed native Claude Code `2.1.268`. The separately verified Anthropic signed-release manifest's `darwin-arm64` SHA-256, `06a96d5423f83770f120859f1c58e60d7252cc4c122aa13043b7e7cd716bc76a`, matched the native-installer file. The published GPG signing-key fingerprint was `31DDDE24DDFAB679F42D7BD2BAA929FF1A7ECACE`; detached-manifest verification reported a good signature. The adapter fixes the manifest hashes for supported Darwin/Linux arm64/x64 variants, rehashes the separately installed ordinary executable before each attempt, and never looks up a release binary on PATH. The proprietary executable is neither a project dependency nor an AGPL redistribution. The source/package audit is in [M2.5 feasibility](M2_5_FEASIBILITY.md).

The live selection was Anthropic `claude-opus-4-8` with the end user's `subscription` source. The owner separately authenticated the unmodified Claude Code CLI using its normal Claude.ai login. Model-free preflight admitted only `loggedIn:true`, `authMethod:claude.ai`, `apiProvider:firstParty`, and `subscriptionType:max`; it discarded account identity and organization data. Immediately before live use, the checked-in probe reported exact release `2.1.268`, `subscription-source-confirmed`, `absent-by-conservative-gate`, both workspace modes, native structured output, and `modelCalls:0`. API-key mode requires the operator's ambient key, invokes official `--bare`, never falls back to Claude.ai login, and was not used for an API-billed live call.

## Native feasibility gates

| Brief gate                               | Result and limit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Official executable                   | Exact native-installer `2.1.268` path, file type, executable bit, canonical path, and signed-manifest hash verified. Missing, symlinked, or mismatched releases reject before a run. A check-to-spawn replacement window remains a documented limit.                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2. Ordinary customization and file tools | The source adapter ran the attested native binary with `--safe-mode --restricted` and synthetic localhost responses. Planted user/project `CLAUDE.md`, project skill, custom agent, and SessionStart hook did not enter model requests or execute; native init reported zero MCP servers/plugins and expected tools. Native tool probes confirmed inside Read/Write and blocked outside, symlink, and protected-state reads/writes. API-key `--bare` exposed Read/Edit only: an existing inside file changed while outside, symlink, and protected targets did not. Bare mode cannot claim Write or new-file creation.                                      |
| 3. Managed policy                        | The fail-closed model-free gate rejects endpoint-managed directories, matching managed-preference files, a present effective macOS Claude Code domain, unreadable/unrecognized inventories, and synthetic managed hooks/tools/instructions. The owner macOS had eight unrelated generic profiles but no effective Claude Code domain or matching file. `profiles show -all` requires root, so this is a conservative effective-domain/file gate. Personal Pro/Max status excludes documented Team/Enterprise-only remote delivery. Policy appearing between preflight and spawn remains a reviewed limit.                                                   |
| 4. Authentication source                 | Subscription rejects ambient `ANTHROPIC_API_KEY`, provider/base-URL, and configuration overrides before its allowlisted status check; the child receives no API key. API-key mode requires an operator key and official `--bare`, excluding normal OAuth/keychain login. No OAuth/keychain value is opened or copied. The owner reviewed the local-direct interpretation in ADR 0015; Anastom offers no login, request proxy, credential relay, subscription resale, or fallback between sources.                                                                                                                                                           |
| 5. Native result and evidence            | Shared conformance and process doubles prove valid, invalid, and missing schema outcomes; nonzero/native failure; premature EOF; split multibyte, malformed, and oversized JSONL; bounded public queue; partial usage; identity provenance; private sentinel exclusion; and terminal-result immutability. The native localhost HTTP 400 produced a sanitized provider failure after one fake POST. Malformed fake StructuredOutput caused ten fake POSTs and a sanitized native failure; the repeated-request cause remains unknown. Result-level usage is partial and cannot establish billing completeness. `--max-turns` is separate from Task duration. |
| 6. Process ownership                     | Committed doubles cover initialized/active cancellation, surviving descendants, TERM-resistant leader/descendant, late writes, concurrent attempts, and zombie-only groups. Success requires confirmed group cleanup. Both runtime process suites pass on Linux and macOS. A pre-existing Codex helper had treated zombie-only groups as live under a short deadline; the owner rejected a rerun as acceptance. Commit `e206059` now inspects live group members on both platforms, uses a bounded one-second reap window, and passed 50 local oversized-frame samples plus first-attempt macOS CI.                                                         |

The normal suite has no model credentials or provider calls. At `e206059`, ten independent Codex process-test runs passed 13/13 tests each, including 50 oversized-frame cleanup samples. The full suite passed **28 files / 211 tests** outside the local process sandbox. `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm hygiene`, and `git diff --check` passed; `pnpm docs:api` generated warning-free HTML for all package entries.

Final-code CI passed without rerunning the cleanup fix: [Linux and macOS](https://github.com/gsornsen/anastom/actions/runs/35053602651), [CodeQL analysis](https://github.com/gsornsen/anastom/actions/runs/35053601450), [CodeQL PR alerts](https://github.com/gsornsen/anastom/runs/104659194434), [DCO](https://github.com/gsornsen/anastom/actions/runs/35053602663), and [dependency review](https://github.com/gsornsen/anastom/actions/runs/35053602659). Earlier CodeQL runs identified three operator/fixture path reads, then the shared helper and later artifact read. The implementation added lexical and canonical containment, fixed the test marker, rejected symlinked artifact parents, and simplified the normalized guard into CodeQL's recognized sanitizer shape. The final alert gate is green without a suppression.

## Provider-backed portability demonstration

The run used the unchanged M2 Task in fresh clone `/private/tmp/anastom-m25-claude-live-20260915`. Its base was `9550094734ab43faef9af29edb1f747167ba7e89`; the exact Task bytes had SHA-256 `d857563cc411565f2cc0718905841c3e4faddd1481a52095b45b59aea1457e93`. The source checkout was clean before and after the run.

Run `776f0606-e56a-4d52-97f8-1cc44f92ed21` succeeded on its first attempt. It negotiated runtime `claude-code` with isolated and readonly workspace modes, cancellation, streaming, native structured output, partial usage, and restricted-working-directory sandboxing; browser, debugger, LSP, MCP, native subagents, and resumable sessions were false. The configured and reported identities both matched `anthropic/claude-opus-4-8` at runtime version `2.1.268`.

Public partial usage recorded inclusive input `20,806`, output `811`, cache-read input `13,285`, and cache-write input `7,515`. Reasoning and total counters were unavailable. These result-level observations do not claim complete request, cost, or billing coverage.

The control-plane verifier exited zero in 156 ms, with 208 stdout bytes, zero stderr bytes, and two tests passing with zero failures. Only `server.mjs` changed in the retained isolated worktree. The later-process artifact verifier reopened the store without Claude Code or provider authentication, recomputed all **9/9** referenced SHA-256 digests, checked artifact producer/ID consistency, re-read the command-result/stdout/stderr acceptance evidence, confirmed the source clone stayed clean, and compared every retained diff artifact to fresh Git diff bytes. It reported 34 ordered events, the retained worktree, a matching fresh diff, and `modelCalls:0`.

Later-process `status` and `inspect --json` both reconstructed the succeeded run. Its definition digest is `sha256:fdbc84d509e6b1a70e9c021e778c108b86873d62b696d8a265bbd8b214e6cca2`. Definition digests vary because `sourcePath` preserves each clone's absolute Task location. Removing only that provenance field and hashing canonical sorted JSON plus a newline produced portable workflow SHA-256 `663306c4e4c2c6ab753be19224c85686f6362f2ff5aa90baafe50728cfaac3dd`, exactly matching the retained M2 Pi and Codex inspections. This establishes a common Task contract, not comparative model quality.

## Durable compatibility replay

The checked-in [historical replay script](../scripts/check-historical-replay.ts) called the current reader's later-process `status` and `inspect --json`, then compared raw workflow/event row bytes before and after. It made zero model calls. Event-row hashes join ordered `event_json` rows with a newline; they are a comparison convention distinct from the earlier M2 evidence digest convention.

| Original store     | Run ID                                 | Replayed status | Events | Workflow-row SHA-256                                               | Ordered event-row SHA-256                                          |
| ------------------ | -------------------------------------- | --------------- | -----: | ------------------------------------------------------------------ | ------------------------------------------------------------------ |
| M1 accepted        | `fac27741-93ee-4c78-9ac6-21d0e97dc465` | succeeded       |     27 | `1c5467b7068edd61fcc1fdd340d9fc1ae6b53c02f872fe4d38d7d69040ccf25b` | `32e933d51af8c4c412023eed0975ce91c25a4a922e4d205db4dced9557290c8c` |
| M1 verifier failed | `a35a40c7-e876-44d0-a010-1a6b0b6ee0a4` | failed          |     27 | `1c5467b7068edd61fcc1fdd340d9fc1ae6b53c02f872fe4d38d7d69040ccf25b` | `768bb7affa86a4a07979b597b891ef6f7d55c1a79763fca1348a3f9ed83d6128` |
| M2 Pi              | `3412a292-dff6-43a7-a979-f506d7ba424d` | succeeded       |     38 | `e2eef0a411a760f93298915c45a44b76bf2de8061a4ef9971dd85eb1ebca9d14` | `a73970c9f5eb2f5a9a8ee7a40c10807de2dc589b996d6755d5db2fb4131ec481` |
| M2 Codex           | `e5b25329-897d-4c1b-b399-97b2438c10c2` | succeeded       |     34 | `f662cda8201776bdd4dd4a2e68b0c1ac8fefa899e3f8dcd293844b99d8748199` | `882c51e8324ecb48bec0bfe556bfd8f0951202caa6b110f8fe3d09e5b36a11b5` |

All four row-digest pairs were unchanged. The new adapter adds no SQLite table, applied-migration edit, Task schema change, or runtime-contract schema change. The retained M2 Pi run was also rechecked through the tightened artifact reader: all 9/9 artifacts, two HTTP tests, clean source, and fresh diff still passed.

## Path-policy decision and completion boundary

[ADR 0016](adr/0016-path-policy-after-third-adapter.md) compares operator sources, scenario file children, verifier `cwd`, run artifacts, Codex auth linking, both executable-provenance profiles, managed-policy inventory, and Claude Code's private native tool inputs. The narrow `@anastom/path-policy` existing-child resolver serves Fake `fromFile`, verifier `cwd`, and scoped core Task/Workflow/schema loaders where canonical-root, containment, internal-symlink, and existing-type rules match. The CLI treats its current directory as the authored source root; its breaking SemVer plan is explicit. Artifact later reads separately reject symlinked run-owned parents. Installed executables, native model file tools, auth stores, managed-policy inventory, and artifact creation retain their distinct policies.

The owner reviewed and approved the code after the CodeQL remediation and deterministic cleanup fix. Contribution rights are permanently confirmed for owner-authored Anastom work. Final owner review of this evidence record remains before M2.5 is marked complete and PR #21 is merged.

# M2 portable-worker evidence

Status: paired live demonstration and offline feasibility passed locally. **M2 is not complete.** Final Linux/macOS CI, CodeQL, and owner review of the completed implementation PR remain required.

## Identities and environment

The accepted M2 design is [PR #11](https://github.com/gsornsen/anastom/pull/11), merged as `8659117`. Codex discovery/authentication refinement [PR #15](https://github.com/gsornsen/anastom/pull/15) merged as `c5ba209`; client-compaction refinement [PR #18](https://github.com/gsornsen/anastom/pull/18) merged as `e9217e5`. The implementation branch's first signed-off checkpoint is `54389aaa9b985df8213e7a0fa3f0273ddc818df6`; the production managed-policy gate followed at `ded0cfcab988dc45fbf46801cb3bc35e18f30a13`. The owner accepted [ADR 0014](adr/0014-codex-managed-policy-preflight.md) on 2026-09-15 before its live use; the signed-off status/path correction is `2131e367b70b7007f13d6ad437ff5ae0343b5f9a`. The final implementation commit and [PR #19](https://github.com/gsornsen/anastom/pull/19) result will be recorded after CI passes.

The local offline checks used Node.js `26.4.0`, pnpm `11.9.0`, macOS Darwin `25.2.0` on arm64, `@openai/codex@0.154.0` with native `codex-cli 0.154.0`, and upstream source tag `rust-v0.154.0` at `6b9826e3aa83b1a5947db50f4332cb9c65f1b340`. Routine CI targets Node.js 24 and Linux/macOS; its results belong to the implementation PR. The Codex live selection is OpenAI `gpt-5.6-terra` at medium effort; Pi remains Anthropic `claude-opus-4-8`.

## Deterministic checks so far

The full repository suite passed outside the enclosing process sandbox: 22 test files and 171 tests. `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm hygiene`, and `git diff --check` passed after the managed-policy and discovery changes. Warning-free TypeDoc HTML was generated for `core`, `runtime-contract`, `engine`, `runtime-pi`, `runtime-codex`, `persistence`, `workspaces`, and `cli`. The original YAML example validated, rendered its three-node graph, and completed the scripted fake run successfully.

The final local suite after auth-path and Darwin process changes passed 22 files and 172 tests. A previous sandboxed run failed at `process.kill(-pid, 0)` with macOS `EPERM` during owned-group confirmation; two macOS CI runs reproduced that result, although their Linux suites passed. The candidate now inspects live process-group members with fixed-argument `/bin/ps` only when Darwin group-wide `kill(0)` returns `EPERM`; unknown or timed-out inspection still rejects cleanup. A focused macOS fixture forces that outcome and verifies descendant termination. The accepted 100 ms SIGTERM escalation and 300 ms confirmation limits remain in force. Final CI is pending.

The exact native [offline audit](M2_FEASIBILITY.md) covers the adapter's system instructions and report schema, synthetic user/project/ancestor instructions and symlinked/explicit skills, synthetic file-backed ChatGPT refresh through the original auth symlink, one-request HTTP/SSE provider exhaustion, schema-preserving accumulated context, and managed cloud conflicts. The owner-file-backed policy probe ran again after the owner's Pro upgrade, used the normal Codex login, found no managed policy, and made zero model calls; it printed only a whitelisted authentication-form and policy result. The actual production policy gate rejected synthetic managed requirements and enterprise configuration before a model request. Shared conformance and process doubles test public evidence filtering, outcome validation, cancellation and surviving descendants, framing, pressure, and policy rejection with no credentials or model calls.

## Paired live endpoint demonstration

The owner approved ADR 0014, its evidence, and the live-test gate before these model calls. Pi used Anthropic `claude-opus-4-8`; Codex used OpenAI `gpt-5.6-terra` at medium reasoning effort with normal file-backed authentication. Both successful runs compiled unchanged `tasks/add-endpoint.md` bytes at SHA-256 `d857563cc411565f2cc0718905841c3e4faddd1481a52095b45b59aea1457e93` from fresh local clones at base commit `9550094734ab43faef9af29edb1f747167ba7e89`. The clones were clean before and after execution; separate retained isolated worktrees each changed only `server.mjs`.

| Runtime | Successful run ID                      | Clone                                        | Durable full-definition digest                                            | Independent verifier            |
| ------- | -------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------- |
| Pi      | `3412a292-dff6-43a7-a979-f506d7ba424d` | `/private/tmp/anastom-m2-paired-pi`          | `sha256:e2eef0a411a760f93298915c45a44b76bf2de8061a4ef9971dd85eb1ebca9d14` | exit 0; tests 2, pass 2, fail 0 |
| Codex   | `e5b25329-897d-4c1b-b399-97b2438c10c2` | `/private/tmp/anastom-m2-paired-codex-rerun` | `sha256:f662cda8201776bdd4dd4a2e68b0c1ac8fefa899e3f8dcd293844b99d8748199` | exit 0; tests 2, pass 2, fail 0 |

The full-definition digests differ only because `WorkflowDefinition.sourcePath` records each clone's absolute Task path. Comparing the normalized workflow objects after omitting only that provenance field produced identical canonical sorted JSON bytes and SHA-256 `663306c4e4c2c6ab753be19224c85686f6362f2ff5aa90baafe50728cfaac3dd` in both later-process inspections. The digest includes the canonical JSON file's newline; `cmp` found the bytes identical. The [brief](M2_BUILD_BRIEF.md) now states this path-specific definition versus portable contract distinction. No runtime selection or outcome field was removed from the workflow projection because neither is stored there.

The accepted `RuntimeNegotiated` records use `anastom.dev/runtime-negotiation/v1alpha1` and the same requirements: isolated workspace, cancellation true, and validated structured output. Pi recorded `prompted` output, streaming true, partial usage reporting, and no declared OS sandbox; Codex recorded `native` output, streaming true, partial usage reporting, and read-only/workspace-write sandboxing. Both declared readonly and isolated support. Their attempt metadata recorded **configured**, rather than provider-reported, identities: Pi `anthropic/claude-opus-4-8` with Pi runtime `0.85.1`; Codex `openai/gpt-5.6-terra` with native runtime `0.154.0`. Durable inspection used the recorded snapshots and identities rather than contacting either runtime.

Pi recorded one partial attempt usage observation: input 12,047, output 669, cache read 8,551, cache write 3,488, total 12,716; reasoning was unavailable. Pi's SDK splits uncached input, cache read, and cache write into disjoint parts; the adapter included the cache parts once in input and retained the SDK's positive total. Codex recorded one partial observation: input 7,997, output 545, cache read 3,072, reasoning 44; cache write and total were unavailable. Pinned JSONL input/output are inclusive of their documented cache-read/reasoning subsets; the adapter omits synthesized zero and ambiguous cache-write/total fields. These counters describe observed calls, not prices or comparable model quality. The verifier command has no provider usage in either run.

The independent `node --test server.test.mjs` verifier passed outside each worker sandbox with HTTP 200 JSON `{"status":"ok"}` and unmatched-route 404 tests; Pi's command exit took 131 ms and Codex's 155 ms. Codex's worker report noted that its own attempted loopback test was blocked by sandbox `EPERM`; that report did not decide acceptance. The control-plane verifier passed after the worker returned its schema-valid report. Each later-process `status` returned succeeded with one successful implement and verifier attempt; each later-process `inspect --json` replayed Pi's 38 and Codex's 34 ordered events without a Pi/Codex executable or provider session. `RuntimeNegotiated` followed `RunCreated` before `AttemptStarted` in both histories.

A first Codex clone/run `72dd4aa8-c368-4b3a-aa81-da3d7661a6c9` changed `server.mjs` but ended as `model-provider` before a native final report; the engine blocked verification and did not retry the attempt. A separate bounded read-only model diagnostic then succeeded on the selected model/profile, and a fresh clone at the same base/Task digest produced the successful Codex run above. The native failure's exact upstream cause was not retained in public evidence, so this record does not attribute it more specifically. The failed source checkout also remained clean and its worktree was retained.

An independent local verifier read every `ArtifactProduced` URI under its run-owned state root, recomputed SHA-256 from bytes, checked the digest and artifact ID, and checked producer/run ownership: 9/9 Pi and 9/9 Codex artifacts matched. The `verify-1-diff` artifact bytes also matched fresh `git diff` bytes from each retained worktree; both worktrees show only `M server.mjs`. The empty verifier-stderr artifact matched SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` in each run. The command-result and stdout artifacts yielded the independent exit/pass counts above. Verification used `/private/tmp/anastom-m2-verify-artifacts.mjs` against the later-process JSON inspections, without reading provider stores.

Both runtimes met the same engineering contract: explicit Task, accepted capability preflight, fresh isolated worker state, one validated report, one independent verifier, typed event history, clean source checkout, retained diff, and later durable replay. Observed differences were the configured harness/model identity, prompted versus native output constraint, sandbox availability, public token subsets, event count, generated implementation bytes, and duration. The two diffs and reports are intentionally not byte-identical; no comparative model-quality claim follows from this small fixture.

## Original M1 store replay

A local clone at original M1-compatible commit `c5ba2093057df5c938e9eda05a7774caf431efa7` created two distinct SQLite histories using its own installed dependencies and fake Task runner. The newer reader reconstructed both histories in later processes without an executable or provider session:

| Original history         | Run ID                                 | New reader                                                    | Events | Event-row SHA-256 before/after                                     | Workflow-row SHA-256 before/after                                  |
| ------------------------ | -------------------------------------- | ------------------------------------------------------------- | -----: | ------------------------------------------------------------------ | ------------------------------------------------------------------ |
| Endpoint accepted        | `fac27741-93ee-4c78-9ac6-21d0e97dc465` | `status` succeeded; JSON `inspect` replayed                   |     27 | `0eae394b672d9004f7ce4c7d31e0dd0f19c3d90b9bd71a091fc7fba141699cc9` | `5ab7c61cabb0ae3605df19f03bf44cbca6f035ed5f70ef70fe48f5c7f7a9e3cf` |
| Endpoint verifier failed | `a35a40c7-e876-44d0-a010-1a6b0b6ee0a4` | `status` failed with verifier exit 1; JSON `inspect` replayed |     27 | `7403f3aed57b492745db7528c57feb5c9e770d260be801ecdf24a5defdec4e87` | `5ab7c61cabb0ae3605df19f03bf44cbca6f035ed5f70ef70fe48f5c7f7a9e3cf` |

The original stores remain in the local temporary fixtures ending `UDUsNX` and `FD6Piz` under the system temp root. The hashes cover ordered `event_json` and `workflow_json` row bytes, not SQLite's mutable page layout. Both before/after pairs matched exactly.

## Remaining completion evidence

Re-run all required local checks and warning-free API documentation on the final code, obtain passing Linux/macOS portability and CodeQL CI on the final implementation commit, record that commit and CI identities here, and request the owner's implementation PR review. No M2.5 or M3 work is included.

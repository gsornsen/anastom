# Parallel normalizers fixture

This deliberately incomplete repository exercises Anastom's defined SDLC with two independent implementation tasks. `feature.md` is the operator contract, and `acceptance.mjs` is the protected deterministic verifier. A successful run implements both modules on isolated task branches, integrates their accepted patches, obtains independent reviews, and leaves the result on a run-owned integration branch.

---
"@anastom/cli": minor
---

Compose strict repository-bound Feature documents and the built-in `sdlc/default` methodology through the production durable CLI, with protected source inputs, a retained integration branch, existing runtime selection, and the bounded live stream.

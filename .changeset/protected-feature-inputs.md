---
"@anastom/core": minor
"@anastom/cli": patch
---

Let Feature authors declare repository-relative protected paths for operator-owned verifier inputs and other immutable policy files. Merge them with CLI-selected Feature and methodology inputs without guessing that command arguments are paths.
